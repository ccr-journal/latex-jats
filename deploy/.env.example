# Domain name for the web service (Caddy handles TLS automatically)
SITE_ADDRESS=jats.example.com

# Docker Hub username/org (must match the images pushed by CI)
DOCKERHUB_USERNAME=yourusername

# Image version (set to a specific release tag, or "latest")
VERSION=latest

# Optional: override default ports
# HTTP_PORT=80
# HTTPS_PORT=443
